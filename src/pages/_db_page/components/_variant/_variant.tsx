import { FC, ReactNode } from 'react';
import classes from './classes.module.scss'

type Props = {
  
}

const _variant:FC<Props> = () => {
  return(
    <div className={classes.wrapper}></div>
  )
}

export default _variant